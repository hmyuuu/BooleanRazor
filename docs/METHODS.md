<!-- GENERATED; DO NOT EDIT. Source: reports/data/project.json SHA-256: 6e71dd99c9ce7a4d228a5c231a3a4f6abf7e9d1fce56eefeea5a0945b062b8f8; report generator SHA-256: 834732d86d134fe0b7934590f6e4d12bbec7f10b883c49feaa2f285c10e101bd -->

# Methods

| Method | Status | Insight | Optimization | Stop rule |
| --- | --- | --- | --- | --- |
| Truth-table and XAG backend | verified_main | Accuracy eligibility precedes reachable gate count. Exhaustive replay binds the emitted XAG to its completed table. | Canonicalize and eliminate unreachable gates before scoring. Retain deterministic table, circuit, and artifact serialization. | Stop on any training inconsistency, table/circuit mismatch, nondeterminism, or non-improving gate count. |
| Arithmetic controls | verified_main | The four v1 functions are disclosed controls, not blind-learning targets. Their gate counts are constructive upper bounds. | Use the controls to guard semantics and serialization while optimizing new methods on permitted synthetic or visible-only data. | Never use a disclosed function label or control recovery as evidence for a blind candidate. |
| Complemented ROBDD | verified_main | Complemented edges and shared output roots reduce representation without changing exact semantics. | Search variable orders under the final reachable-XAG score, not raw BDD nodes alone. | Reject any complemented-edge invariant violation or exhaustive replay mismatch. |
| Care-BDD | verified_main | Visible-row completion and variable-order search must remain fold-local during selection. Fairer scheduling fixed coverage but did not improve the frozen synthetic score. | Prefer hypotheses that can improve exact-row completion rather than gate-only tuning at zero exact-row CV. Use the frozen five folds, reuse-sibling policy, and 32-evaluation budget for the public gate. | Reject on visible training inconsistency, exhaustive inequivalence, budget drift, or loss to both frozen baselines. |
| Bounded SAT resynthesis | verified_main | UNSAT proves only the exact frozen local encoding and bound. Timeout and Unknown remain censored observations. | Apply six-input local rewrites only to an already exact candidate. Require whole-XAG equivalence after reinsertion. | Stop after two successive Timeout or Unknown outcomes, any inequivalence, or no strict gate reduction. |
| OxiDD oracle | verified_main | An independent reduced oracle can catch ordering and semantic drift without becoming the production learner. | Use only for reduced checks and finalist order comparisons. | Never use OxiDD as the production learner or challenge gate metric. |
| Bounded experiment runner | verified_main | Only SUCCESS is runner success; VERIFIER_NOT_RUN can preserve candidate artifacts without claiming an official pass. Failed and timed-out cells are evidence, not missing rows. | Keep each algorithm cell under 300 seconds including startup and cleanup. Bind source, tree, run spec, artifacts, logs, timing, memory, and terminal state. | Reject missing, malformed, overwritten, or source-unbound evidence. |
| Official Julia wrapper | verified_main | Official verification is a distinct authority from internal exhaustive equivalence. Wrapper availability does not itself verify a candidate. | Create immutable records only inside the authorized custodian boundary and bind every verifier input. | Fail on malformed output, metric mismatch, extra output, unsafe paths, file replacement, or nonzero Julia exit. |
| ProjectedSupportBDD | verified_branch_only | Per-output projected supports changed exact-row synthetic generalization where global-order tuning had not. The final bound pair reproduced 104857/104857 exact rows and 72 gates. | Use R2 as the leading input to a separate public-candidate integration review. Preserve executable self-binding and fold-local support selection. | Reject any source/executable mismatch, support conflict, fold leakage, nondeterminism, or exhaustive inequivalence. |
| Tensor-network pilot | verified_branch_only | A tensor model becomes comparable only after full prediction enumeration and the same Rust XAG backend. The final pair proved pipeline repeatability on a fully observed parity fixture. | Evaluate a future candidate only on permitted visible inputs and preserve the Rust authority boundary. | Reject device drift, unbound executable bytes, stream overflow, incomplete enumeration, or Rust equivalence failure. |
